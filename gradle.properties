pluginGroup = com.sakurastudios.lsl
pluginName = sakura-lsl
pluginVersion = 1.0.0

# Platform versions
javaVersion = 17
kotlinVersion = 1.9.24
platformType = IC
platformVersion = 2024.1
pluginSinceBuild = 241
# Wide upper bound: every IntelliJ-platform release we've seen since 2024.1
# (build 241) is still compatible with our minimal Kotlin surface area.
# Bump this only if a future build actually breaks something.
pluginUntilBuild = 299.*

# Gradle
org.gradle.jvmargs = -Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel = true
org.gradle.caching = true

# Kotlin
kotlin.stdlib.default.dependency = false
kotlin.incremental.useClasspathSnapshot = true
